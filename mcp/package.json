{
  "name": "@frontier/mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol server exposing Frontier market/maker/taker/position/lens tools (describe + execute, with tx simulation).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "frontier-mcp": "./dist/server.js"
  },
  "files": [
    "dist",
    "README.md",
    "manifest.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@frontier/sdk": "file:../sdk",
    "viem": "^2.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
