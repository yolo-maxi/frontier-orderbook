# GOAL: Harmonize the feature vocabulary to "mirror liquidity" across the entire repo

Repo: `/home/xiko/frontier-worktrees/rename-mirror` (branch `chore/rename-mirror`, off `main`). Foundry root: `prototype/`. Also: `ui/` (Vite/React/TS app), `website/` (VitePress docs), `landing/` (Vite landing), `README.md`.

## Context
The mirror-liquidity feature currently goes by TWO inconsistent names: **"shadow"** (internal/contracts) and **"copy liquidity"** (UI/product). Fran wants ONE term everywhere: **"mirror liquidity"** (identifier stem `mirror`). Rename BOTH source vocabularies to mirror.

This CHANGES THE CONTRACT ABI (function selectors + event names). Contracts, tests, and the UI's ABI/calls must all move together and stay consistent. We are redeploying afterward, so ABI change is fine.

## Rename mapping — apply in THIS ORDER (compound/specific first to avoid double-replacement)

### A. Contracts + tests (prototype/), first-party only — NEVER touch prototype/lib/
Handle compound names FIRST so you don't produce `mirrorMirror*`:
- `mirrorShadowBid` → `mirrorBid`, `mirrorShadowAsk` → `mirrorAsk` (collapse the redundant doubled word)
- `quoteBuyShadowed` → `quoteBuyMirrored`, `quoteSellShadowed` → `quoteSellMirrored`, any `…Shadowed` → `…Mirrored`, `Shadowed` → `Mirrored`
Then the public API (ABI — keep selectors consistent across contracts↔tests↔UI):
- `shadowReserves` → `mirrorReserves`, `shadowReserve0/1` → `mirrorReserve0/1`
- `depositShadow` → `depositMirror`, `depositShadowFor` → `depositMirrorFor`, `_depositShadow` → `_depositMirror`
- `withdrawShadow` → `withdrawMirror`
- `shadowSharesOf` → `mirrorSharesOf`, `shadowShares` → `mirrorShares`, `shadowTotalShares` → `mirrorTotalShares`
- `zapDepositShadow` → `zapDepositMirror`, `previewZapDepositShadow` → `previewZapDepositMirror`
- events: `ShadowDeposit` → `MirrorDeposit`, `ShadowWithdraw` → `MirrorWithdraw`
- fees: `shadowFee` → `mirrorFee`, `ShadowFee` → `MirrorFee`, `payShadowFee` → `payMirrorFee`, `ShadowFeeTransferFailed` → `MirrorFeeTransferFailed`
- remaining: `shadowPaid` → `mirrorPaid`, `shadowOut` → `mirrorOut`, `grossShadowOut` → `grossMirrorOut`, and any other `shadow`/`Shadow` identifier → `mirror`/`Mirror`
- comments/NatSpec prose: "shadow" → "mirror" (and where it describes the product, "mirror liquidity")

### B. UI (ui/src) — rename the "copy liquidity" AND any "shadow" terminology to "mirror liquidity"
- Component `CopyLiquidityPane` → `MirrorLiquidityPane` (file + symbol + imports). Any `copyLiquidity`/`CopyLiquidity` identifiers tied to THIS feature → `mirrorLiquidity`/`MirrorLiquidity`.
- ABI definitions / contract call names in `ui/src/abi/*` and callers must match the renamed contract functions/events exactly (`zapDepositMirror`, `previewZapDepositMirror`, `mirrorReserves`, `depositMirror`, `withdrawMirror`, `MirrorDeposit`, etc.).
- User-visible copy: "Copy Liquidity" / "Shadow" → "Mirror Liquidity".
- ⚠️ DO NOT rename unrelated uses of the word "copy" (e.g. copy-to-clipboard, `navigator.clipboard`, "copy address"). Only the liquidity feature.

### C. Docs (website/) + landing (landing/) + README
- Replace "shadow"/"copy liquidity" feature references with "mirror liquidity".
- Rename `website/experiments/copy-liquidity.md` → `website/experiments/mirror-liquidity.md` and update the VitePress nav/sidebar link + any internal links pointing to it.
- ⚠️ Again, don't touch unrelated "copy" wording.

### D. NEW DOCS SECTION (explicitly requested by Fran)
Add a clear explanation of **Uniform vs Geometric books** to the docs (best home: `website/guide/pricing.md` or `website/guide/topology.md` — pick the most fitting, or add a short dedicated page and link it in the sidebar). Cover:
- Uniform = linear/constant-absolute tick spacing; ideal for bounded ranges like **prediction markets** (probability 0–1, evenly-spaced levels).
- Geometric = constant-percentage spacing (`1.0001^tick`, Uniswap-v3-style); ideal for wide multiplicative price ranges (spot tokens).
- Note that mirror liquidity works on BOTH book types (the rebalance preview routes through the lens's curve-aware quoting).
Keep it accurate to the code; ground it in the actual contracts.

## EVALUATION GATES — loop until ALL pass or document a real blocker
- `cd prototype && forge build` — clean.
- `cd prototype && forge test` — **271 passed, 0 failed, 2 skipped** (same as before the rename; renames must not change behavior).
- `cd prototype && forge build --sizes` AND `FOUNDRY_PROFILE=deploy forge build --sizes` — both books still ≤24,576 B.
- UI: `cd ui && CI=true npx vite build` — clean build (run `pnpm install` first if node_modules missing; or a symlink may already be present).
- Docs: `cd website && CI=true npx vitepress build` — clean, no dead links (the renamed experiments page link must resolve).
- Landing: `cd landing && CI=true npx vite build` — clean.
- **Residual check**: `grep -rni "shadow" prototype/src prototype/test ui/src website landing README.md` returns ZERO (excluding prototype/lib). And no remaining feature-sense "copy liquidity" in ui/website/landing (generic "copy"/clipboard is fine).

## Reporting & boundaries
- Commit in logical chunks on `chore/rename-mirror`. Do NOT push, open PRs, or broadcast on-chain. Do NOT touch prototype/lib/.
- Write a final report to `prototype/RENAME_MIRROR.md`: what was renamed (with the compound-name handling), files touched per area, all gate results, residual-grep result, and any blockers.
